services:
  notebooks:
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        - GETML_VERSION_NUMBER=$GETML_VERSION_NUMBER
    networks:
      - notebooks_network
    ports:
      - "1709:1709"
      - "8888:8888"

networks:
  notebooks_network: